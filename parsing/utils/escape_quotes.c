#include "../parsing.h"

const char	*escape_quotes(const char *literal)
{
    return (literal);
    // const char *end;
    // const char *quote;
    // const char *start;
    
	// while (contains_quotations(literal))
	// {

	// }
}