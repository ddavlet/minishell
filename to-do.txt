Have to fix:

1. "<<" tokens in quotations
2. parenthesis:
argv[0] = minishell
argv[1] = -r
3. check "test" command

4. Change wildcard $ is one more token

5. export must work with "" too (example: export ARG="test")
